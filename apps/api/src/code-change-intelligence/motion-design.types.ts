/**
 * Motion plan aligned with performance and reduced-motion (Design Brain V2).
 */
export type MotionDesignPlan = {
  entrance: string;
  hoverFocus: string;
  transitions: string;
  loading: string;
  microInteractions: string;
  reducedMotion: string;
  performanceNotes: string;
};
